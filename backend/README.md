# Log Management API

This is a backend API for a log management and security monitoring system, built with Python and FastAPI.

## 🚀 Prerequisites

Before you begin, ensure you have the following installed on your local machine:

* **Python 3.8+**
* **PostgreSQL** (The database must be installed, running, and accessible)
* **Git** (for cloning the repository)

## 🛠️ Local Setup Instructions

Follow these steps to get the application running on your machine.
```bash
pip install -r requirements.txt
```
Then run 
```bash
uvicorn main:app --reload
```
## ‼️Addtional
### You **MUST** create a database file
You need to download pgadmin4 download script of query from folder db then run it
### You **MUST** create .env file
You can create .env file following .env.example put your database connect and password then a table and things will work file
